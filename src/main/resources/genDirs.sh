#!/usr/bin/env bash

root="assets/KotlinMod"
models="${root}/models"
textures="${root}/textures"

mkdir -p ${root}

mkdir ${models}
mkdir ${textures}

mkdir ${models}/item
mkdir ${models}/block

mkdir ${textures}/items
mkdir ${textures}/blocks